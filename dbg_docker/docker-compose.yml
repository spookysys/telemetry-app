
version: '2'

services:
  telemetry-app:
    image: dbg_docker:latest
    build: .
    environment:
      NODE_ENV: development
    ports:
      - 8080:8080
      - 5858:5858
      - 13610:13610
    volumes:
      - ..:/usr/src/app
    command: node --inspect=13610 --debug-brk --nolazy app.js
