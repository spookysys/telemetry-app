{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run locally",
            "type": "node2",
            "request": "launch",
            "program": "${workspaceRoot}/app.js",
            "cwd": "${workspaceRoot}"
        },
        {
            "name": "Run w/docker",
            "type": "node2",
            "request": "attach",
            "preLaunchTask": "compose up",
            "localRoot": "${workspaceRoot}",
            "remoteRoot": "/usr/src/app",
            "port": 13610
        }
    ]
}